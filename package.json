{
  "name": "damka",
  "version": "1.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install --include=dev && npx prisma generate && npm run build",
    "start": "cd server && npx prisma migrate deploy && node dist/index.js"
  }
}
